<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Be My Valentine ❤️</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      font-family: 'Segoe UI', cursive;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, #ff758c, #ff7eb3);
      overflow: hidden;
    }

    .card {
      background: white;
      padding: 40px 50px;
      border-radius: 20px;
      text-align: center;
      box-shadow: 0 20px 40px rgba(0,0,0,0.2);
    }

    h1 {
      color: #ff4d6d;
      margin-bottom: 10px;
    }

    p {
      color: #555;
      font-size: 18px;
      margin-bottom: 30px;
    }

    .buttons {
      position: relative;
      height: 60px;
    }

    button {
      padding: 12px 28px;
      font-size: 18px;
      border-radius: 30px;
      border: none;
      cursor: pointer;
      transition: all 0.2s ease;
    }

    #yesBtn {
      background: #ff4d6d;
      color: white;
      margin-right: 20px;
    }

    #noBtn {
      background: #ddd;
      color: #333;
      position: absolute;
    }

    #popup {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.6);
      display: none;
      align-items: center;
      justify-content: center;
    }

    .popup-card {
      background: white;
      padding: 40px;
      border-radius: 20px;
      text-align: center;
      max-width: 350px;
      animation: pop 0.4s ease;
    }

    .popup-card h2 {
      color: #ff4d6d;
    }

    @keyframes pop {
      from { transform: scale(0.7); opacity: 0; }
      to { transform: scale(1); opacity: 1; }
    }
  </style>
</head>
<body>

  <div class="card">
    <h1>Hey Pookie 💖</h1>
    <p>Will you be my Valentine and make my world even more beautiful?</p>

    <div class="buttons">
      <button id="yesBtn">Yes 💘</button>
      <button id="noBtn">No 🙈</button>
    </div>
  </div>

  <div id="popup">
    <div class="popup-card">
      <h2>Yayyy!! 🥹❤️</h2>
      <p>
        You just made me the happiest person alive.  
        I promise endless love, laughs, and forehead kisses 💕
      </p>
      <p>Happy Valentine’s Day, my love 🌹</p>
    </div>
  </div>

<script>
  const noBtn = document.getElementById("noBtn");
  const yesBtn = document.getElementById("yesBtn");
  const popup = document.getElementById("popup");

  function moveNoButton() {
    const btnWidth = noBtn.offsetWidth;
    const btnHeight = noBtn.offsetHeight;

    const x = Math.random() * (window.innerWidth - btnWidth - 20);
    const y = Math.random() * (window.innerHeight - btnHeight - 20);

    noBtn.style.left = `${x}px`;
    noBtn.style.top = `${y}px`;
  }

  // Desktop
  noBtn.addEventListener("mouseover", moveNoButton);

  // Mobile
  noBtn.addEventListener("touchstart", (e) => {
    e.preventDefault(); // prevents click
    moveNoButton();
  });

  yesBtn.addEventListener("click", () => {
    popup.style.display = "flex";
  });
</script>


</body>
</html>
